// Copyright Sierra

/* eslint-env node */
module.exports = {
    extends: [
        "eslint:recommended",
        "plugin:@typescript-eslint/recommended",
        "plugin:@typescript-eslint/recommended-requiring-type-checking",
        "plugin:react/recommended",
        "plugin:react/jsx-runtime",
        "plugin:react-hooks/recommended",
        "plugin:import/recommended",
        "prettier",
    ],
    parser: "@typescript-eslint/parser",
    parserOptions: {
        project: true,
        tsconfigRootDir: __dirname,
    },
    plugins: ["@typescript-eslint"],
    root: true,
    settings: {
        "react": {
            version: "detect",
        },
    },
    rules: {
        // Built-in rules

        // Enable optional built-in rules that flag likely mistakes.
        "array-callback-return": "error",
        "no-constant-binary-expression": "error",
        "no-constructor-return": "error",
        "no-promise-executor-return": "error",
        "no-self-compare": "error",
        "no-template-curly-in-string": "error",
        "no-unmodified-loop-condition": "error",
        "no-unreachable-loop": "error",
        "require-atomic-updates": [
            "error",
            {
                allowProperties: true,
            },
        ],
        "no-eval": "error",
        "no-implied-eval": "error",
        "no-new-object": "error",
        "no-new-wrappers": "error",
        "no-script-url": "error",
        "no-useless-rename": "error",
        // Strict equality is less error-prone than loose equality.
        "eqeqeq": "error",
        // Object shorthand is more concise.
        "object-shorthand": [
            "error",
            "always",
            {
                avoidQuotes: true,
            },
        ],
        // Template strings are more readable than string concatenation.
        "prefer-template": "error",
        // Disallow console logs (since they are usually temporary debugging).
        "no-console": [
            "error",
            {
                allow: [
                    "warn",
                    "error",
                    "group",
                    "groupCollapsed",
                    "groupEnd",
                    "trace",
                    "time",
                    "timeEnd",
                ],
            },
        ],

        // Disable built-in "no-duplicate-imports" in favor of import/no-duplicates
        "no-duplicate-imports": "off",
        "import/no-duplicates": ["error", { "prefer-inline": true }],

        // disable import/no-unresolved since we use a non-standard resolver
        "import/no-unresolved": "off",

        // @typescript-eslint rules

        // Disable or relax recommended rules that we don't agree with.
        "@typescript-eslint/no-explicit-any": "off",
        "@typescript-eslint/no-non-null-assertion": "off",
        "@typescript-eslint/no-unsafe-argument": "off",
        "@typescript-eslint/no-floating-promises": "off",
        "@typescript-eslint/no-empty-function": "off",
        "no-constant-condition": ["error", { checkLoops: false }],
        // Allow async functions without await, because we sometimes implement
        // interfaces that expect async, and it's nice to be consistent even if
        // a specific implementation does not use away.
        "@typescript-eslint/require-await": "off",
        // Similarly, don't complain about unused arguments, so that when
        // implementing interfaces we can keep all arguments, even if a specific
        // implementation does not use them.
        "@typescript-eslint/no-unused-vars": [
            "error",
            {
                args: "none",
                varsIgnorePattern: "^_",
            },
        ],
        // We sometimes want to give a type annotation as additional
        // documentation, even if there is an initializer.
        "@typescript-eslint/no-inferrable-types": "off",
        "no-inner-declarations": "off",
        // Allow unused promises for void return functions (e.g. event handlers,
        // so that we can use async functions for them).
        "@typescript-eslint/no-misused-promises": [
            "error",
            {
                "checksVoidReturn": {
                    "arguments": false,
                    "attributes": false,
                },
            },
        ],
        // Allow numbers and booleans in template strings -- useful when
        // debugging and have straightforward serializations.
        "@typescript-eslint/restrict-template-expressions": [
            "error",
            {
                allowNumber: true,
                allowBoolean: true,
            },
        ],

        // Enable additional rules that we agree with (either for safety or
        // readability/terseness reasons).
        "curly": ["error", "all"],

        // Enable preferring of type imports, with the inline style so that we
        // don't require two separate import statements.
        "@typescript-eslint/consistent-type-imports": [
            "error",
            {
                prefer: "type-imports",
                disallowTypeAnnotations: false,
                fixStyle: "inline-type-imports",
            },
        ],
        // Also prefer type exports.
        "@typescript-eslint/consistent-type-exports": [
            "error",
            {
                fixMixedExportsWithInlineTypeSpecifier: true,
            },
        ],
        // Optional chaining is less verbose than `&&` checks.
        "@typescript-eslint/prefer-optional-chain": "error",
        // Flag unnecessary optional chaining.
        "@typescript-eslint/no-unnecessary-condition": [
            "error",
            {
                allowConstantLoopConditions: true,
            },
        ],
        // T[] is terser and more readable than Array<T>.
        "@typescript-eslint/array-type": "error",
        // Prefer type annotations for Map and other generic types in the
        // constructor, to avoid repeating the type.
        "@typescript-eslint/consistent-generic-constructors": "error",
        // Prefer index signatures over the Record type because naming the index
        // is helpful documentation.
        "@typescript-eslint/consistent-indexed-object-style": ["error", "index-signature"],
        // Avoid duplicate enum values (likely to be copy/paste errors).
        "@typescript-eslint/no-duplicate-enum-values": "error",
        // Ensure enums are of a consistent type.
        "@typescript-eslint/no-mixed-enums": "error",
        // Avoid comparing enums to numbers (which may never match)
        "@typescript-eslint/no-unsafe-enum-comparison": "error",
        // Maps should be used instead of objects for dictionaries, especially
        // ones that are mutated.
        "@typescript-eslint/no-dynamic-delete": "error",
        // Prefer nullish coalescing over ||.
        "@typescript-eslint/prefer-nullish-coalescing": "error",
        // ...but avoid unnecessary nullish coalescing.
        "@typescript-eslint/no-non-null-asserted-nullish-coalescing": "error",
        // Prefer shorter type names (if the type argument is the default).
        "@typescript-eslint/no-unnecessary-type-arguments": "error",
        // Prefer `!` over the more verbose `as ...` syntax.
        "@typescript-eslint/non-nullable-type-assertion-style": "error",
        // Prefer for...of loops.
        "@typescript-eslint/prefer-for-of": "error",
        // Prefer `includes` over `indexOf(...) === -1` (since it's shorter).
        "@typescript-eslint/prefer-includes": "error",
        // Prefer returning `this` instead of repeating the class name in
        // chained methods.
        "@typescript-eslint/prefer-return-this-type": "error",
        // Prefer `startsWith`/`endsWith` over `indexOf` (since it's shorter).
        "@typescript-eslint/prefer-string-starts-ends-with": "error",
        // Require explicit sorting functions for arrays, since the
        // toString() done by default is surprising/incorrect.
        "@typescript-eslint/require-array-sort-compare": ["error", { ignoreStringArrays: true }],
        // Flag unnecessary boilerplate.
        "@typescript-eslint/no-useless-constructor": "error",
        // GraphQLError (from the graphql package) has a custom toString()
        // implementation, but it's not part of the type declaration, so the
        // rule doesn't know about it.
        "@typescript-eslint/no-base-to-string": [
            "error",
            {
                ignoredTypeNames: ["Error", "RegExp", "URL", "URLSearchParams", "GraphQLError"],
            },
        ],

        // eslint-plugin-react rules
        "react/hook-use-state": "error",
        "react/jsx-fragments": "error",
        "react/jsx-key": ["error", { checkFragmentShorthand: true }],
        "react/jsx-no-constructed-context-values": "error",
        "react-hooks/exhaustive-deps": "error",
        // Props are checked via TypeScript, and this rule has false positives
        // with nested functions.
        "react/prop-types": "off",
        // Adds too much boilerplate when using quotes.
        "react/no-unescaped-entities": "off",
    },
    ignorePatterns: [
        // Ignore generated code.
        "/build/**",
        "main.js",
        // Ignore this file (it's not part of the TypeScript project).
        "/.eslintrc.cjs",
    ],
};
